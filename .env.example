# Supabase Configuration
# These environment variables are required for the application to function
# Copy this file to .env and fill in the values

# Supabase Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous/Public Key (safe to expose to frontend)
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Gmail SMTP Configuration (recommended for email notifications)
# Create an App Password in your Gmail account settings
GMAIL_USER=yourgmail@gmail.com
GMAIL_PASS=your-gmail-app-password

# Alternative SMTP Configuration (if not using Gmail)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USER=admin@yourdomain.com
SMTP_PASS=your-smtp-password

# Email System Configuration
# Default sender email for notifications
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
DEFAULT_FROM_NAME=Your Business Name

# Business Information (used in email templates)
BUSINESS_NAME=Your Business Name
BUSINESS_PHONE=+1-234-567-8900
BUSINESS_EMAIL=contact@yourdomain.com

# Note: Never commit the actual .env file to version control
# The .env file should contain your actual project values
